<h1>Welcome to the BURGER LOUNGE!</h1>

<h2>Cooked Burgers ready to DEVOUR!!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger-block eat=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>What Burger would you like to order?!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger-block eat=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Create YOUR Burger HERE!</h2>
<form class="create-form" action="/api/burgers" method="POST">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input required type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="slee">Hungry?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Hungry
  </div>

  <button type="submit">Add Burger!</button>
</form>

